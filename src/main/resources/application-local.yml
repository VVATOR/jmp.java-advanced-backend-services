spring:
  datasource:
    url: jdbc:h2:mem:local
    username: local_user
    password: local_pass
  jpa:
    show-sql: true
    properties:
      format_sql: true
    hibernate:
      ddl-auto: create
logging:
  level:
    org.springframework.security: DEBUG