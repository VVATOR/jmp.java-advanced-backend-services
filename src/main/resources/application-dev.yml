spring:
  datasource:
    url: jdbc:h2:mem:dev
    username: dev_user
    password: dev_pass
  jpa:
    properties:
      format_sql: true
    hibernate:
      ddl-auto: update
logging:
  level:
    org.springframework.security: DEBUG