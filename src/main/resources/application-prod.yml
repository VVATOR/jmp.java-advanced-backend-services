spring:
  datasource:
    url: jdbc:h2:mem:prod
    username: prod_user
    password: prod_pass
  jpa:
    show-sql: false
    properties:
      format_sql: true
    hibernate:
      ddl-auto: none
logging:
  level:
    org.springframework.security: error